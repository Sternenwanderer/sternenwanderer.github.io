---
layout: layouts/base.njk
section: home
permalink: "/index.html"
---
<article>

  <header class="pb--4">

    <div class="grid grid--intro">

      <div class="grid__item--span-4 mw--readable markdown">{{ settings.metadata.site_description | renderUsingMarkdown | safe }}</div>

    </div>

    {% if reservierung.verkauf %}
      <a href="/reservierung/" class="button">🎟️ Jetzt Karten für {{ reservierung.titel }} reservieren 🎟️</a>
    {% endif %}

  </header>

  

  {% set productionList = collections.productions | limit(settings.theme.theme_features.item_output_limit or 48) %}
  {% include "components/productionList.njk" %}

</article>

{# Appended to <head> during build #}
{% include "components/opengraph.njk" %}